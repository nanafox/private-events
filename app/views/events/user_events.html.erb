<h2 class="text-xl font-semibold">Events Organized by <%= @creator.fullname %></h2>

<section id="events" class="grid lg:grid-cols-4 md:grid-cols-1 gap-2 md:gap-2 lg:gap-4 mt-4">
  <%= render @events %>
</section>
